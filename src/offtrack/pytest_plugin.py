"""pytest plugin for offtrack (entry point ``pytest11``).

Full fixture/assertion API lands with the plugin milestone; this module must
import cleanly from day one because the entry point is always registered.
"""

from __future__ import annotations
